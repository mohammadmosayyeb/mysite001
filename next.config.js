/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: [

            "image.mosayyebnezhad.ir",
            "liara.ir",
            "cdn.dribbble.com"
        ]
    }
}

module.exports = nextConfig
